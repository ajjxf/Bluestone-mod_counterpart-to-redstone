# Open the private vanilla RedStoneWireBlock#shouldSignal (Yarn: wiresGivePower) field so
# PurplestoneWireBlock can read/write it directly without an @Accessor mixin.
# (NeoForge 1.20.2 moddev plugin uses Mojang official mappings, so Mojmap names are used here.)
public net.minecraft.world.level.block.RedStoneWireBlock shouldSignal
